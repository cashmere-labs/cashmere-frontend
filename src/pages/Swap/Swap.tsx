import { Footer, Navbar } from "components";
import { Layout } from "ui";
import styles from "./Swap.module.scss";

const Swap = () => {
  return (
    <Layout>
      <Navbar />
      <Footer />
    </Layout>
  );
};

export { Swap };
